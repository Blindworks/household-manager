# Development profile configuration
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/household_manager_dev?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    drop-first: false

logging:
  level:
    com.household: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
