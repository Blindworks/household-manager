# Test profile configuration
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/household_manager_test?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  liquibase:
    enabled: true
    drop-first: true

logging:
  level:
    com.household: INFO
    org.springframework: WARN
    org.hibernate: WARN
