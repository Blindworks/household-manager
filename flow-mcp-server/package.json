{
  "name": "flow-mcp-server",
  "version": "1.0.0",
  "description": "MCP server wrapping the Household-Manager flow engine REST API so AI agents can create, update, deploy and toggle flows.",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  }
}
